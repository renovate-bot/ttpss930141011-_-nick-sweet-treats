version: "3.9"
services:
  db:
    image: postgres
    volumes:
      - postgres-db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=medusa_db
      - POSTGRES_USER=ttpss930141011
      - POSTGRES_PASSWORD=abc5383101
    ports:
      - 5432:5432
volumes:
  postgres-db: